/*******************************************************************************
 * Communication_cfg.h - 통신 설정 (프로젝트별 프로토콜 상수)
 *
 * 기능:
 *   - 프로토콜 프레임 상수 (STX, ETX)
 *   - 패킷 크기 정의 (RX/TX)
 *   - FW 버전 번호
 *   - 타이머 주기 설정값
 *
 * 계층 구조:
 *   Application(pmsm.c) → Config(이 파일) + Core(Communication)
 *   Communication.c, protocol.c, Communication_drv.c에서 참조
 *
 * 프로젝트 이식 시 수정 포인트:
 *   - 프로토콜 프레임 상수, 패킷 크기, FW 버전 값 조정
 ******************************************************************************/
#ifndef COMMUNICATION_CFG_H
#define COMMUNICATION_CFG_H

#ifdef __cplusplus
extern "C" {
#endif

/*=============================================================================
 * 프로토콜 프레임 상수
 *===========================================================================*/
#define COMM_STX            0x40    /* STX = Start of Text */
#define COMM_ETX            0x2A    /* ETX = End of Text */

/*=============================================================================
 * 패킷 크기 정의
 *===========================================================================*/
#define COMM_PACKET_MIN_LEN     4       /* 최소 패킷 길이 (STX + cmd + ETX + checksum) */
#define COMM_RX_PACKET_LEN      18      /* RX 패킷 길이 (수신) */
#define COMM_TX_PACKET_LEN      20      /* TX 패킷 길이 (송신) */
#define COMM_RX_ASSEMBLY_SIZE   256     /* RX 조립 버퍼 크기 */

/*=============================================================================
 * FW 버전
 *===========================================================================*/
#define COMM_VERSION_FW     105     /* FW = Firmware 버전 번호 */

/*=============================================================================
 * 타이머 설정값
 *===========================================================================*/
#define COMM_TX_INTERVAL_MS     100     /* TX 응답 전송 주기 (ms) */
#define COMM_HEALTH_PERIOD_MS   1000    /* 통신 건강 상태 판단 주기 (ms) */

#ifdef __cplusplus
}
#endif

#endif /* COMMUNICATION_CFG_H */
